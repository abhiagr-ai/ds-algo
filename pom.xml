<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.8.1</version> <!-- Use an appropriate version -->
            <configuration>
                <source>25</source> <!-- Your desired Java source version -->
                <target>25</target> <!-- Your desired Java target version -->
            </configuration>
        </plugin>
    </plugins>
</build>